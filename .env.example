# ===========================================
# Bluebox Docker Configuration
# ===========================================
# Copy this file to .env and customize as needed
#
# Usage:
#   cp .env.example .env
#   docker-compose up -d

# PostgreSQL version (15, 16, 17, 18, or 19-dev)
# 18 = current stable, 19-dev = development/master branch
PG_VERSION=18

# Superuser password (for postgres user)
POSTGRES_PASSWORD=password

# Port mapping (change if 5432 is in use)
# PG_PORT=5432

# ===========================================
# Application credentials (set by init scripts)
# ===========================================
# These are created automatically, listed here for reference:
#
# Schema admin (DDL, maintenance):
#   User: bb_admin
#   Password: admin_password
#
# Application (DML, queries):
#   User: bb_app
#   Password: app_password
#
# To change these, modify init/01-create-roles-and-database.sql
